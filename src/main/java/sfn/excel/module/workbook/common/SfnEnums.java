package sfn.excel.module.workbook.common;

public class SfnEnums {
    public enum CellType {
        NUMERIC,
        STRING,
        ERROR,
        BLANK,
        FORMULA,
        BOOLEAN,
        DEFAULT,
        CUSTOM_PARSE_ERROR
    }
}
